# experiment
run_name: dense_debug
model_key: bert_dense

# data
batch_size: 32
max_length: 128

# model
model_name: bert-base-uncased
dropout: 0.1

# train
learning_rate: 2e-5
epochs: 3
seed: 42

# io
output_root: outputs
