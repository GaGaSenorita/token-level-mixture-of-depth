# Experiment
run_name: dense_bert_finetune_agnews
model_key: finetuned_dense_bert

# Data
batch_size: 32
max_length: 128

# Model
model_name: bert-base-uncased
dropout: 0.1

# Train
learning_rate: 2e-5
epochs: 3
seed: 42

# IO
output_root: ./outputs